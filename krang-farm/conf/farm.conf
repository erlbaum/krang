# This is the configuration file for the Krang Farm.  See
# docs/adding_a_new_machine.pod for details about what you need to
# configure here.

# each machine gets a Machine block
<Machine Redhat7_3>

   # a reminder of what's on this machine
   Description "Redhat 7.3 Server"

   # the user and password the farm system will use to login, needs sudo
   User krang
   Password krang

   # the Perl binaries and the builds they generate
   Perls /usr/bin/perl Redhat7_3-perl5.6.1-i386-linux

</Machine>

# each machine gets a Machine block
<Machine Redhat7_3_i686>

   # a reminder of what's on this machine
   Description "Redhat 7.3 Server w/ custom Perls for i686"

   # the user and password the farm system will use to login, needs sudo
   User krang
   Password krang

   # the Perl binaries and the builds they generate
   Perls /usr/bin/perl Redhat7_3-perl5.6.1-i686-linux \
         /usr/local/bin/perl5.6.2 Redhat7_3-perl5.6.2-i686-linux \
         /usr/local/bin/perl5.8.3 Redhat7_3-perl5.8.3-i686-linux \
         /usr/local/bin/perl5.8.4 Redhat7_3-perl5.8.4-i686-linux

</Machine>

# each machine gets a Machine block
<Machine Redhat9>

   # a reminder of what's on this machine
   Description "Redhat 9 Server"

   # the user and password the farm system will use to login, needs sudo
   User krang
   Password krang

   # the Perl binaries and the builds they generate
   Perls /usr/bin/perl Redhat9-perl5.8.0-i386-linux-thread-multi

</Machine>

# each machine gets a Machine block
<Machine Redhat9_i686>

   # a reminder of what's on this machine
   Description "Redhat 9 Server with Custom Built i686 Perls"

   # the user and password the farm system will use to login, needs sudo
   User krang
   Password krang

   # the Perl binaries and the builds they generate
   Perls /usr/local/bin/perl5.6.1 Redhat9-perl5.6.1-i686-linux \
         /usr/local/bin/perl5.6.2 Redhat9-perl5.6.2-i686-linux \
         /usr/local/bin/perl5.8.3 Redhat9-perl5.8.3-i686-linux \
         /usr/local/bin/perl5.8.4 Redhat9-perl5.8.4-i686-linux 

</Machine>

# each machine gets a Machine block
<Machine Fedora1>

   # a reminder of what's on this machine
   Description "Fedora Core 1 Server"

   # the user and password the farm system will use to login, needs sudo
   User krang
   Password krang

   # the Perl binaries and the builds they generate
   Perls /usr/bin/perl FedoraCore1-perl5.8.1-i386-linux-thread-multi \
         /usr/local/bin/perl5.8.3 FedoraCore1-perl5.8.3-i686-linux \
         /usr/local/bin/perl5.8.4 FedoraCore1-perl5.8.4-i686-linux 

</Machine>

# each machine gets a Machine block
<Machine Fedora2>

   # a reminder of what's on this machine
   Description "Fedora Core 2 Server"

   # the user and password the farm system will use to login, needs sudo
   User krang
   Password krang

   # the Perl binaries and the builds they generate
   Perls /usr/bin/perl FedoraCore2-perl5.8.3-i386-linux-thread-multi 

</Machine>


# each machine gets a Machine block
<Machine Gentoo>

   # a reminder of what's on this machine
   Description "Gentoo 2004.2"

   # the user and password the farm system will use to login, needs sudo
   User krang
   Password krang

   # the Perl binaries and the builds they generate
   Perls /usr/bin/perl Gentoo-perl5.8.4-i686-linux

</Machine>

# each machine gets a Machine block
<Machine Fedora3>

   # a reminder of what's on this machine
   Description "Fedora Core 3 Server"

   # the user and password the farm system will use to login, needs sudo
   User krang
   Password krang

   # the Perl binaries and the builds they generate
   Perls /usr/bin/perl FedoraCore3-perl5.8.5-i386-linux-thread-multi

</Machine>

# each machine gets a Machine block
<Machine Fedora4>

   # a reminder of what's on this machine
   Description "Fedora Core 4 Server"

   # the user and password the farm system will use to login, needs sudo
   User krang
   Password krang

   # the Perl binaries and the builds they generate
   Perls /usr/bin/perl FedoraCore4-perl5.8.6-i386-linux-thread-multi

</Machine>

# each machine gets a Machine block
<Machine FreeBSD_5_4>

   # a reminder of what's on this machine
   Description "FreeBSD 5.4"

   # the user and password the farm system will use to login, needs sudo
   User krang
   Password krang

   # the Perl binaries and the builds they generate
   Perls /usr/bin/perl FreeBSD-perl5.8.6-i386-freebsd-64int

</Machine>

# each machine gets a Machine block
<Machine RedHat_ES4>

   # a reminder of what's on this machine
   Description "RedHat Enterprise Server 4"

   # the user and password the farm system will use to login, needs sudo
   User krang
   Password krang

   # the Perl binaries and the builds they generate
   Perls /usr/bin/perl RHEL_ES4-perl5.8.5-i386-linux-thread-multi

</Machine>

